# Shared

Shared contracts live here.

- `types/`: API request/response types and domain types used by frontend and backend.

Change these files intentionally because they define the contract between branches.
